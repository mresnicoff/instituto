import { config } from 'dotenv';

// Cargar las variables de entorno
config();

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes añadir configuraciones específicas para Next.js si las necesitas
};

export default nextConfig;